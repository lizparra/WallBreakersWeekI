class Solution {
    public boolean isPowerOfTwo(int n) {
        int end = (int)Math.ceil(Math.sqrt(n));
        for(int i = 0; i <= end; i++){
            if(Math.pow(2,i) == n){
                return true;
            }
        }
        return false;
    }
}
